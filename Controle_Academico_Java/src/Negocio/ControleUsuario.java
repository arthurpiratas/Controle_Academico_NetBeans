package Negocio;

/*import Basicas.Administrador;
import Basicas.Aluno;
import Basicas.Professor;
import Negocio.ControleADM;
import Negocio.ControleAluno;
import Negocio.ControleProfessor;*/

public class ControleUsuario {
	/*
	
	public boolean verificaLoginUsuario(ControleADM ctrAdm , ControleAluno ctrAluno, ControleProfessor ctrProfessor , String login, String senha) {
		
		if(repAdm.verifiaADMLogin(login)) {
			Administrador adm = repAdm.buscaADMLogin(login); 
			if(adm.getSenha().equals(senha)) {
				return true; 
			}
		}
		
		return false; 
	}
	public Administrador buscaADMLogin(ControleADM ctrAdm , ControleAluno ctrAluno, ControleProfessor ctrProfessor, String login, String senha) {
		Administrador adm = null;
		
		if (verificaLoginAdm(login, senha)) {
			adm = repAdm.buscaADMLogin(login);
		}
		
		return adm; 
	}
	*/
}
